import { RouteObject } from "../types";

const matchRoutes = (routes: RouteObject[]) => {
  const location = window.location.pathname;
  console.log("location", location);

  return;
};

export default matchRoutes;
